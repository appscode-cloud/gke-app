apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ace-mp.fullname" . }}-test-connection"
  labels:
    {{- include "ace-mp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: wget
    image: {{ .Values.tester.image }}
    command:
    - true
    # command:
    # - curl
    # - -k
    # - "https://ace-ingress-x-ace-x-{{ .Release.Name }}"
  restartPolicy: Never
